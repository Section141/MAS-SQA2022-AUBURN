name: Commit
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  check:
    runs-on: windows-11
    steps:
      - uses: actions/checkout@v3.0.2

      - uses: actions/setup-python@v3.10.0
        with:
          python-version: "3.10"

      - run: python fuzz.py
        shell: bash
